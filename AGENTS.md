# 项目级开发规则

## 1. 适用范围与依据

- 本文件适用于仓库内所有代码、测试、文档、Manifest 和打包脚本的新增与修改。
- 多平台插件化的目标架构、接口草案、迁移顺序和完成定义以 [doc/社交评论清理器多平台插件化解耦合重构方案.md](doc/社交评论清理器多平台插件化解耦合重构方案.md) 为准。
- 方案是分阶段迁移计划，不代表当前代码已经完成解耦。实施任何阶段前，先阅读方案相关章节、检查 `git status --short --branch`，再确认当前实现和测试基线。
- 若实现与方案或现有安全行为冲突，优先保留可验证的安全行为，并在代码或提交说明中记录偏差原因；不得默默改变删除语义。

## 2. 总体架构边界

目标结构由三层组成：

- `core/`：通用会话生命周期、候选队列、状态机、等待取消、节奏、限频、锁、统计、消息路由和 UI 快照。
- `platform/<id>/`：平台 URL 和页面匹配、登录/权限检查、评论面发现、展开/分页、评论解析、菜单/删除动作及平台错误分类。
- `shared/`：与平台无关的纯数据结构、规则、节奏、退避和限频工具。

必须遵守以下依赖方向：

- 核心只能依赖 `PlatformPlugin` 契约、标准记录和 `ActionResult`，不得依赖平台目录内部实现。
- `core/` 不得出现 Instagram 或其他平台的 URL 片段、CSS 选择器、DOM 查询、菜单文本、多语言词表、评论链接格式或平台错误码。
- Service Worker 不自行解析平台路径；目标 URL、页面状态和平台权限必须通过注册中心/插件处理。
- 插件返回数据和明确结果，不直接修改 `TaskSession`、核心统计、核心队列或 UI 状态。
- 插件私有数据可以通过 `CommentRecord.platform` 原样往返，但核心不得读取或解释其中字段。
- 新增平台应新增独立插件目录和注册项，不复制核心运行时、候选策略、面板或节奏模块，也不新增 `if (platform === '...')` 作为长期分支。

## 3. 插件契约规则

每个插件必须通过注册中心注册，并提供稳定的 `id`、显示名、页面匹配规则、能力声明及契约所需的方法组：

- `identity`：目标 URL 规范化、页面匹配、目标上下文、当前账号和内容作者。
- `preflight`：登录、页面状态、目标和删除权限检查。
- `surface`：评论面发现、可滚动面、观察器、稳定快照和可见性判断。
- `loader`：回复/评论展开、加载更多、进度、结束判断和取消。
- `comments`：统一 `CommentRecord` 转换、父子关系、线程构建、作者和元素定位。
- `actions`：元素解析、回复可见、悬停点、菜单、删除确认和删除结果验证。
- `errors`：平台异常到标准错误类别的分类和用户提示。

插件方法必须：

- 返回可校验的标准数据或 `ActionResult`，不吞掉异常。
- 对不支持的能力返回 `unsupported`，对不确定的页面状态返回 `ambiguous` 或 `unknown`，不得猜测后继续删除。
- 支持会话取消；等待、观察器和页面动作在暂停/停止后必须可释放。
- 不把 DOM 节点、菜单文本、账号名称或平台私有字段写入长期会话存储。

统一 `CommentRecord` 至少包含 `id`、`parentId`、`kind`、作者、文本、作者保护标记和必要的短期元素引用；核心只使用标准字段。删除只有在插件 `verifyDeleted()` 明确成功后，核心才可增加 `deleted`、写入 `processedIds` 或继续下一个候选。

## 4. 必须保持的安全行为

- Preview 只扫描、筛选和统计，绝不打开删除菜单、确认删除或改变页面内容。
- 正式执行前必须完成目标、登录、权限和内容作者校验；账号/作者无法确认时暂停。
- 一级评论默认不删除；回复先于父级处理。白名单、作者评论保护和关键词策略属于核心通用规则，不得被平台插件绕过。
- 找不到唯一元素、菜单项不明确、页面重绘导致目标失效、删除结果无法确认、挑战/验证/限流或未知异常时，立即暂停并给出可理解的原因。
- Pause、Stop、Resume、任务锁、锁续租、节奏、全局限频、会话删除数和最长运行时间的语义必须与重构前一致。
- 继续使用 DOM 和页面可见 UI 完成平台动作，不调用 Instagram 私有接口或依赖不可验证的网络协议。
- 真实删除验证只能使用 Preview、小批量和测试账号；静态测试不得宣称已经证明真实平台删除成功。

## 5. 会话、等待与 UI 规则

- 用显式 `TaskSession` 表示会话状态，状态变更通过 `pause()`、`stop()`、`complete()`、`fail()` 等方法完成；UI 只能读取 `getSnapshot()`。
- `CleanerRuntime` 负责通用扫描、候选处理和生命周期编排；它不得直接调用 `document.querySelector()` 或匹配平台文本。
- 所有固定延迟、条件轮询、稳定采样和 Mutation 等待统一经 `WaitCoordinator`，并绑定会话 `AbortSignal`。不得用一个共享计时器覆盖多个并发等待。
- 面板只消费 `UiSnapshot`，按钮只发送 `start`、`preview`、`pause`、`stop` 等核心命令；面板不读取评论 DOM、不调用插件方法、不自行修改业务状态。
- 状态提示不能改变操作按钮尺寸或布局；错误应在操作区上方清晰显示，必填 URL 错误应标记为红色无效字段。

## 6. 后台、配置与 Manifest

- 消息统一使用 `SC_*` 名称，并显式携带 `platformId` 和规范化目标 URL。迁移期间可保留 `ICC_*` 兼容别名，但必须注明删除版本和转换位置，不得永久维护双重接口。
- 锁键、限频键和会话键必须包含平台标识及规范化目标，避免跨平台串任务。
- 设置页通过注册中心生成平台选项、显示名、URL placeholder、能力开关和平台专属字段；通用节奏和限制不得按平台复制。
- 配置只保存规范化值，不保存 DOM、菜单文本或账号名称。平台专属配置放在 `platformOptions`。
- 第一阶段继续使用 Manifest 脚本数组和 `globalThis` 暴露方式，先稳定契约再评估打包器。每个插件必须在页面不匹配时只注册元数据，不执行扫描。
- 新增平台时只增加该平台实际需要的 `matches`、`host_permissions`、插件默认值、测试和文档，不提前放开所有平台域名或权限。

## 7. 分阶段迁移规则

按方案的阶段 0 至阶段 6 小步提交，每阶段都可独立回滚：

1. 先建立契约、注册中心、统一数据结构和回归基线。
2. 再提取 URL、标签、错误分类、解析器、评论面和页面动作。
3. 然后拆分 `TaskSession`、`CleanerRuntime`、等待协调和 UI 快照。
4. 最后通用化后台/设置页/消息协议，再以第二个平台做试点。

提取旧函数时，不得直接复制整段实现；先固定输入、输出、异常和副作用，再由核心通过插件接口调用。不要在解耦阶段同时恢复或扩大自动分页，分页能力应由插件声明并单独验证。兼容层必须有明确删除期限。

## 8. 测试与验收

每次修改至少根据影响范围运行：

- `node --check`：覆盖修改过的 JS、插件、核心、后台和打包脚本。
- `npm test`：覆盖规则、节奏、会话状态、候选保护、错误映射和插件契约测试。
- `git diff --check`：提交前必须无空白错误。

插件还必须覆盖 URL/页面匹配、登录/作者/权限、评论面和稳定快照、ID/父子关系、展开/菜单/删除确认、删除结果、节点重绘和歧义暂停。核心测试不得依赖真实 Instagram DOM；真实浏览器验收需记录页面、账号、批量和结果。

完成插件化阶段前，必须确认：`core/` 无平台细节；Service Worker 不解析平台路径；Instagram 的发现、解析、展开、菜单、删除和异常均由插件提供；任一合约插件可接入 `CleanerRuntime`；重构前后的安全行为一致。

## 9. 代码、文档与 Git 规范

- 变量、函数、类和文件名应见名知意；新增或修改的非显然逻辑必须有简洁、同步的中文注释，避免重复描述代码表面行为。
- 用户可见文案、README、变更说明和操作手册使用简明中文，说明支持范围、权限、安装、Preview/Start 差异和风险边界。
- Git commit 必须使用中文、简洁动宾结构，例如 `新增平台插件契约`、`提取评论解析器`。一个提交只完成一个可回滚边界，提交前检查 diff、测试结果和工作区状态。
- 未经明确要求不得重写历史、删除用户文件或改变远程仓库；交付打包时才执行 `npm run package`，并核对 ZIP 内容、校验和、引用和工作区状态。
